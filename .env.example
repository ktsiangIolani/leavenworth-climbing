# Google Sheets ID (already hardcoded in sheetsService.ts but documented here)
VITE_SHEET_ID=1PGJrobk9nE9Uu1cqFCsBvpaNDXXdEhrk_X_n-JIRuBg

# The app uses Open-Meteo for weather — no API key needed.
# Weather data is fetched from https://api.open-meteo.com/ automatically.

# For Vercel deployment: no environment variables are required by default.
# The Google Sheet must remain shared as "Anyone with the link can view".
